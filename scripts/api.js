define(['util/validation'], function(validation) {

    'use strict';

    // Returning the API object
    return {
        check: validation,
        Data: {
            posts: {}
        },
        Models: {},
        modules: {}
    };

});